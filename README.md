# Product Uploader — update feed

Sparkle appcast + release zips for the Distex Product Uploader (internal macOS app).
The app checks `appcast.xml` on the `main` branch; zips live in GitHub Releases.

To ship an update: run `scripts/release-update.sh` in the Product Uploader repo on the dev Mac.
